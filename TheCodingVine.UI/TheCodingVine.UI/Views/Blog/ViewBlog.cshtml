@model TheCodingVine.UI.ViewModels.BlogVM
@{
    ViewBag.Title = "ViewBlog";
}

<br />
<h3 class="text-center">Viewing one of @User.Identity.Name's Blogs</h3>

<div class="row">
    <div class="container-fluid col-lg-10 col-md-10 col-sm-10 table-bordered">
        <h3>@Model.Blog.Title</h3>
        @Html.Raw(Model.Blog.Content)
        <br />
        @foreach (var tag in Model.Blog.SearchTags)
        {
            @(" #" + tag.SearchTagBody + " ");
        }
    </div>
</div>
<p class="text-center">@Html.ActionLink("Return to Blog List","MyBlogs","Blog")</p>
